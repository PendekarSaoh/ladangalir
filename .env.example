# Enable only after the migration, shared login and live access tests succeed.
SUPABASE_STORAGE_ENABLED=false
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
# Server only. Use a secret key or the legacy service_role key. Never NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=
